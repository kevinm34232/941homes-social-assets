# 941 Homes — Social Media Assets

Public image host for 941 Homes LLC's automated social posts. Buffer fetches
images from here by URL (it has no upload API — only `create_post` with a
public `url`). Populated by `social/render.py` in the Murphy AI OS repo.

Nothing sensitive lives here — every file is a graphic already headed to a
public Facebook/Instagram/X post.
